export const publicroute = ["/", "/product/**.*", "/cart", "/empty"];

export const authroute = ["/auth/signin", "/auth/signup"];

export const apiauthprefix = "/api/auth";

export const default_login_redirect = "/setting";
